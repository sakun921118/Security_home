package com.sakun.security_home
/*
import com.robinhood.spark.SparkAdapter

class PowerUsedSparkAdapter (private val dailyData: List<PowerUsed>) : SparkAdapter(){

    override fun getCount() = dailyData.size

    override fun getItem(index: Int) = dailyData[index]

    override fun getY(index: Int): Float {
        val chosenDayData = dailyData[index]
        return chosenDayData.toFloat()
    }
}*/